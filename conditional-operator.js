const team = ['Tyler','Potter'];


team.push('Vikram', 'Rajesh');
team.push('smith');
const size = (team.length <= 4) ? `Team size: ${team.length}` : "To many people on your team"

console.log(size);
